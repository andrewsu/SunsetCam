# copy this file to config.txt, then edit the values for your install

# absolute path to the SunsetCam repo on this host
ROOT="/home/pi/SunsetCam"

# fallback log file. Only scheduler.sh writes here (it runs at 1 AM, before any
# per-run directory exists). SunsetCam.sh and the scripts it calls always write
# to a per-run log at $ROOT/img/<date>[_n]/log instead.
LOG_FILE="/home/pi/SunsetCam/log"

# camera mount orientation in degrees: 0 for an upright module, 180 if the sensor is mounted
# upside down. Passed straight to rpicam-still --rotation, which accepts only 0 or 180.
CAMERA_ROTATION=0
